I want to create a recurrent neural network that will serve as a latent model generating real neural activity. The real neural data is saved in the folder 'data/training'. The units in the RNN will be composed of S, D, R populations and each population will be composed of Group 0, 1, 2 and 3. There will be N = 10 units in each group in a population. The units should be order by S, D, R and within each population by Group 0, 1, 2, and 3. The equation describing the RNN dynamics is dh/dt = -h + W*g(h) + I where g is a ReLU function, W is a recurrent connection matrix and I is the external input.

The readout matrix that connects the units of the RNN and the real neurons in the neural data is constructed as follows. First, there will be a total readout matrix of size (total number of neurons in a session) x (number of units in the RNN). Each row will correspond to the weights assigned to the ordered units in the RNN. There are 33 sessions in total in the neural data and each session should have its own total readout matrix. Only the neural activity of the top cells stored in the files 'cells.npy' is used for training the RNN. So, the actual readout matrix should be a reduced version of the total readout matrix. Select only the rows in the total readout matrix that correspond to the top cells in 'cells.npy'. Then, check the type (S, D or R) and group number (0, 1, 2 or 3) of each top cell and train only the portion of the row that corresponds to the pair (neuron type, group number). Mask out the rest of the row not used for training. Construct the actual readout matrix by putting together the masked rows of the top cells. Multiplying this actual readout matrix with the rectified unit activity g(h) should be the activity in the neural data. 

Make the code as simple as possible. Add explanation so that I can understand how each step described above is implemented. If anything is unclear ask me. Write this code in _8_LatentNetwork.py.